name: module3_task0

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Clone the repository
        uses: actions/checkout@v2

      - name: Execute make help
        run: |
          cd <directory-name>
          make help